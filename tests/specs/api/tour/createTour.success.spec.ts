import { test, expect } from '@playwright/test'

test.describe('Create a tour', () => {
  test('As an admin, I could create a tour', () => {})

  test('As a tour-lead, I could create a tour', () => {})
})
