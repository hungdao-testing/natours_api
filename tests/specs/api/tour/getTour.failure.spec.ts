import { test, expect } from '@playwright/test'

test.describe('Get tours', () => {
  test('Non-authorized user could not get tours', () => {})

  test('Non-authorized user could not get a detailed of a tour', () => {})
})
